StylesPath = .vale/styles
MinAlertLevel = warning

[*.{txt}]
BasedOnStyles = Vale

# Ignore URLs and inline code spans in prose checks.
TokenIgnores = (https?://\S+)|(`[^`]+`)

[README.md]
# README contains many CLI snippets and code fences.
BlockIgnores = (?s)```.*?```
